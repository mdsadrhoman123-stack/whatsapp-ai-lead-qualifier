# workflows/

## This folder is empty on purpose, and it stays empty

No workflow export for **WhatsApp Lead Qualifier** will be published here. Not a sanitised
one, not a partial one, not a screenshot of the canvas.

## Why

The build is the deliverable. A client pays for the node graph, the execution
order, the prompts, the retry logic and the thresholds that took the iterations
to get right. Publishing that — even with the credentials stripped out — hands
it to anyone who wants it, including the next person bidding against me for
similar work. It would also mean the client who funded it paid for something
that is now free.

So the rule is simple, and it applies to every repository in this portfolio:
**what a client paid me to build is not published.** If I would not publish
yours, I will not publish theirs.

## What is published instead

- The problem, in the client's language — [`../docs/01-problem.md`](../docs/01-problem.md)
- What the client experiences, step by step — [`../docs/02-journey.md`](../docs/02-journey.md)
- The flow at a level a client can follow — [`../docs/03-architecture.md`](../docs/03-architecture.md)
- Every failure path, how it is detected, and who hears about it —
  [`../docs/04-failure-handling.md`](../docs/04-failure-handling.md)
- The tools, and why each one over the alternative —
  [`../docs/05-stack.md`](../docs/05-stack.md)
- What is measured, and what is deliberately not claimed —
  [`../docs/06-results.md`](../docs/06-results.md)
- The trade-offs, stated by the person who made them —
  [`../docs/07-limitations.md`](../docs/07-limitations.md)

That is enough to judge whether the engineering thinking is sound, which is the
question a repository like this exists to answer. A copy of the wiring answers a
different question entirely.

## If you want the walkthrough

Ask. On a call I will happily open the real thing and walk through the design
decisions, the parts that failed first, and why the failure paths look the way
they do. That conversation is more useful than a JSON file anyway.

**K MD SAYAD RAHMAN** — [khandokarsayad@gmail.com](mailto:khandokarsayad@gmail.com) · [LinkedIn](https://www.linkedin.com/in/khandokarsayad)
